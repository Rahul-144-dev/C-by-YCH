// WAP in C to calculate the Area of a Triangle.
#include <stdio.h>
int main()
{
  float base = 12, height = 20;
  float area = 0.5 * base * height;
  printf("Area of a triangle is %f", area);
  return 0;
}